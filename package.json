{
    "name": "portfolio",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "hugo-extended --minify",
        "dev": "hugo-extended server"
    },
    "devDependencies": {
        "hugo-extended": "0.118.2"
    }
}
